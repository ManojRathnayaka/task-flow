package com.taskmanager.model.enums;

/**
 * TaskPriority — represents the urgency level of a task.
 * Using an enum ensures only valid priorities are assignable (O principle).
 */
public enum TaskPriority {
    LOW,
    MEDIUM,
    HIGH
}
